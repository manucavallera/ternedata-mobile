server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Expo Web is a single-page application. Deep links, such as email
        # verification, must load the app instead of returning nginx's 404.
        try_files $uri $uri/ /index.html;
    }
}
